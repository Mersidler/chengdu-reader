node_modules/
dist/
web-ext-artifacts/

# ---- 本地密钥与凭据（绝不可提交） ----
# 存放小米 MiMo API Key 的文件。扩展本身不硬编码任何密钥，
# key 只存在浏览器 storage.local 里；这个文件仅供 verify-e2e.cjs 本地调试用。
.tts-key
.tts-key-probe

# ---- 本地开发环境 ----
.web-ext-profile/

# ---- 验证脚本的运行产物（每次运行都会变，不是源码） ----
tests/devtools/*-result.json
